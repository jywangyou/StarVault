# 新增任务

## 基本信息


| 项目     | 内容          |
| -------- | ------------- |
| 负责人   | 忘忧        |
| 创建时间 | 2026-02-07  |
| 状态     | 进行中 |

---

## 任务目标

现在的选择器没生效，点击没反应，修复一下。
侧边栏新增的菜单要有名字，名字直接在图标下面显示出来。
现在新增的任务到时间后并没有提示，看下哪里的问题，我要用chrome.notifications api来实现定时提醒。
新增完成任务后，要在农历的下面显示任务列表，现在任务列表没有显示出来。
任务列表封装成组件，后面可能增加别的列表，比如日记，计划等等，公用一个列表在农历下面。
宽度自适应，足够宽的时候将列表和日历放到同一行，否则放到农历下面。



---

## 进度追踪

### 已完成

- [X]  步骤1：将remixicon换成npm引入
- [X]  步骤2：在侧边栏增加一个图标点击弹出新页面
- [X]  步骤3：获取到日历中当前所选的日期

### 进行中

- [ ]  步骤4：时间选择不生效，修复一下。
- [ ]  步骤5：任务列表没出现，修复一下
- [ ]  步骤6：任务列表自适应宽度，并在足够宽的时候将列表和日历放到同一行，否则放到农历下面，并封装成组件。
- [ ]  步骤7：切换日期显示当前所选日期的任务列表。



### 待完成



### 下一步

> 接下来需要：从步骤4开始，一个一个实现。

---

## 技术方案

整体都用vue3+ts+tailwindcss实现。



---

## 关键决策

### 决策1：使用谷歌插件提醒chrome.notifications api来实现定时提醒

**问题** ：用户新增一个任务后，到了任务时间，需要提醒用户。

**决策** ：使用chrome.notifications api来实现定时提醒。

**理由** ：chrome.notifications api是谷歌浏览器插件的标准API，使用起来比较方便。



---

